// Stampare a schermo un messaggio all’interno di un h1, utilizzando i data.
  // inizializzo vue
new Vue({
    // lo posiziono nell'html
  el: '#root',
  data: {
    esercizioBase: 'Stampare a schermo un messaggio all’interno di un h1, utilizzando i data.',
    message: 'Hello Vue.js!',
  }
})
